// import Player from "./Classes/player.js";
// import Registry from "./registry.js";
// import Camera from "./Services/CameraService.js";
// import RenderService from "./Services/RenderService.js";

// const player = new Player([0, 0], 2*Registry.m, 0, 160, 100, 100);
// const camera = new Camera([0, 0, 5*Registry.m], player.Angle, [129, 90]);
// RenderService.setActiveCamera(camera);
// player.Camera = Registry.activeCamera;

// player.Velocity[0] = 1*Registry.m;
// player.Velocity[1] = 0.5*Registry.m;

// console.log(Registry.activeCamera === player.Camera)